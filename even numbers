// even number
#include <iostream>
using namespace std;

int main() {

	int a,b,i;
	cin >> a >> b;
	if (a<=b && a>=1 && b<=30000){
		for(i=a;i<=b;i++){
			if (i%2 == 0){
				cout << i <<" ";
			}
		}
	}

	return 0;
}
